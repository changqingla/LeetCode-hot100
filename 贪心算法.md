# 贪心算法专题

## 🔍 贪心算法基础知识

### 📖 定义

**贪心算法**（Greedy Algorithm）是一种在每一步选择中都采取当前状态下最优的选择，从而希望导致结果是最优的算法。这种算法不从整体最优上加以考虑，只做出在某种意义上的局部最优解。

### ⚡ 核心特征

1. **局部最优**：每一步都选择当前最优解
2. **不可回溯**：一旦做出选择，就不会回退
3. **无后效性**：当前选择不会影响之前的决策
4. **贪心选择性**：通过局部最优选择达到全局最优

### 🎯 贪心算法的适用条件

1. **最优子结构**：问题的最优解包含子问题的最优解
2. **贪心选择性**：每一步的最优选择能导致全局最优解
3. **无后效性**：未来的决策不受当前决策影响

### 🐍 贪心算法的应用场景

- **区间问题**：活动安排、区间覆盖
- **分配问题**：任务分配、资源分配
- **选择问题**：股票买卖、跳跃游戏
- **构造问题**：字符串分割、数字构造

### 📏 贪心策略示例

#### 1. 活动安排问题
```python
# 选择尽可能多的兼容活动
def activity_selection(activities):
    # 按结束时间排序
    activities.sort(key=lambda x: x[1])

    selected = [activities[0]]
    for activity in activities[1:]:
        if activity[0] >= selected[-1][1]:
            selected.append(activity)

    return selected
```

#### 2. 零钱找零问题
```python
# 使用尽可能少的硬币
def coin_change_greedy(coins, amount):
    coins.sort(reverse=True)  # 从大到小
    count = 0

    for coin in coins:
        count += amount // coin
        amount %= coin

    return count if amount == 0 else -1
```

---

## 📋 题目目录

| 题号 | 题目名称 | 难度 | 核心技巧 |
|------|----------|------|----------|
| 121 | 买卖股票的最佳时机 | 简单 | 单次交易贪心 |
| 55 | 跳跃游戏 | 中等 | 覆盖范围贪心 |
| 45 | 跳跃游戏 II | 中等 | 最小跳跃次数 |
| 763 | 划分字母区间 | 中等 | 区间合并贪心 |

---

## 121. 买卖股票的最佳时机

**题目描述：**

给定一个数组 prices ，它的第 i 个元素 prices[i] 表示一支给定股票第 i 天的价格。

你只能选择 某一天 买入这只股票，并选择在 未来的某一个不同的日子 卖出该股票。设计一个算法来计算你所能获取的最大利润。

返回你可以从这笔交易中获取的最大利润。如果你不能获取任何利润，返回 0 。

**测试用例：**

```
示例 1：
输入：[7,1,5,3,6,4]
输出：5
解释：在第 2 天（股票价格 = 1）的时候买入，在第 5 天（股票价格 = 6）的时候卖出，最大利润 = 6-1 = 5 。

示例 2：
输入：prices = [7,6,4,3,1]
输出：0
解释：在这种情况下, 没有交易完成, 所以最大利润为 0。
```

**最简单实现：**

```python
def maxProfit(prices):
    """
    买卖股票的最佳时机：贪心算法

    思路：
    1. 记录历史最低价格
    2. 遍历数组，计算当前价格卖出时的利润
    3. 更新最大利润
    4. 同时更新历史最低价格

    时间复杂度：O(n)
    空间复杂度：O(1)
    """
    if not prices:
        return 0

    min_price = float('inf')  # 历史最低价格
    max_profit = 0  # 最大利润

    for price in prices:
        # 更新历史最低价格
        min_price = min(min_price, price)
        # 计算当前卖出时的利润
        current_profit = price - min_price
        # 更新最大利润
        max_profit = max(max_profit, current_profit)

    return max_profit

# 动态规划版本
def maxProfitDP(prices):
    """
    动态规划版本：状态机

    思路：
    1. dp[i][0] 表示第i天不持有股票的最大利润
    2. dp[i][1] 表示第i天持有股票的最大利润

    时间复杂度：O(n)
    空间复杂度：O(n)
    """
    if not prices:
        return 0

    n = len(prices)
    dp = [[0] * 2 for _ in range(n)]

    # 初始化
    dp[0][0] = 0          # 第0天不持有
    dp[0][1] = -prices[0] # 第0天持有

    for i in range(1, n):
        dp[i][0] = max(dp[i-1][0], dp[i-1][1] + prices[i])  # 不持有
        dp[i][1] = max(dp[i-1][1], -prices[i])              # 持有

    return dp[n-1][0]

# 测试用例
print(maxProfit([7,1,5,3,6,4]))  # 输出: 5
print(maxProfit([7,6,4,3,1]))     # 输出: 0
```

**解题思路详解：**

这道题的核心是**贪心算法**的应用：

**核心原理：**
- 在最低点买入，在最高点卖出
- 只需要找到全局的最低点和最高点之间的差值
- 贪心策略：记录历史最低价格，实时计算最大利润

**算法流程：**
1. 初始化min_price为无穷大，max_profit为0
2. 遍历价格数组：
   - 更新历史最低价格
   - 计算当前价格卖出的利润
   - 更新最大利润
3. 返回最大利润

**为什么这样有效？**
- 买入时机：历史最低点
- 卖出时机：当前价格（相对历史最低点）
- 这样能保证找到全局最优解

**举例说明：**
```
prices = [7,1,5,3,6,4]

初始化：min_price=inf, max_profit=0

i=0: price=7, min_price=7, profit=7-7=0, max_profit=0
i=1: price=1, min_price=1, profit=1-1=0, max_profit=0
i=2: price=5, min_price=1, profit=5-1=4, max_profit=4
i=3: price=3, min_price=1, profit=3-1=2, max_profit=4
i=4: price=6, min_price=1, profit=6-1=5, max_profit=5
i=5: price=4, min_price=1, profit=4-1=3, max_profit=5

最终结果：5 ✓
```

**时间复杂度证明：**
- 单次遍历数组：O(n)
- 每个元素常数操作：O(1)
- 总复杂度：O(n)

---

## 55. 跳跃游戏

**题目描述：**

给你一个非负整数数组 nums ，你最初位于数组的 第一个下标 。数组中的每个元素代表你在该位置可以跳跃的最大长度。

判断你是否能够到达最后一个下标，如果可以，返回 true ；否则，返回 false 。

**测试用例：**

```
示例 1：
输入：nums = [2,3,1,1,4]
输出：true
解释：可以先跳 1 步，从下标 0 到达下标 1, 然后再从下标 1 跳 3 步到达最后一个下标。

示例 2：
输入：nums = [3,2,1,0,4]
输出：false
解释：无论怎样，总会到达下标为 3 的位置。但该下标的最大跳跃长度是 0 ， 所以永远不可能到达最后一个下标。
```

**最简单实现：**

```python
def canJump(nums):
    """
    跳跃游戏：贪心算法

    思路：
    1. 维护一个变量farthest，表示当前能到达的最远位置
    2. 遍历数组，更新farthest = max(farthest, i + nums[i])
    3. 如果farthest >= n-1，说明能到达终点

    时间复杂度：O(n)
    空间复杂度：O(1)
    """
    if not nums:
        return True

    n = len(nums)
    farthest = 0  # 当前能到达的最远位置

    for i in range(n):
        # 如果当前位置不可达，返回false
        if i > farthest:
            return False

        # 更新最远可达位置
        farthest = max(farthest, i + nums[i])

        # 提前结束：如果已经能到达终点
        if farthest >= n - 1:
            return True

    return farthest >= n - 1

# 另一种贪心思路：反向跳跃
def canJumpReverse(nums):
    """
    反向跳跃：从后往前找能到达终点的起始位置

    时间复杂度：O(n)
    空间复杂度：O(1)
    """
    n = len(nums)
    last_pos = n - 1  # 最后需要到达的位置

    # 从后往前遍历
    for i in range(n - 2, -1, -1):
        # 如果当前位置能跳到last_pos
        if i + nums[i] >= last_pos:
            last_pos = i  # 更新目标位置

    return last_pos == 0

# 测试用例
print(canJump([2,3,1,1,4]))  # 输出: True
print(canJump([3,2,1,0,4]))  # 输出: False
```

**解题思路详解：**

这道题的核心是**贪心算法**的应用：

**核心原理：**
- 维护当前能到达的最远位置
- 贪心策略：每一步都选择能跳得最远的位置
- 如果最远位置 >= n-1，说明能到达终点

**算法流程：**
1. 初始化farthest = 0
2. 遍历数组：
   - 如果当前位置 > farthest，说明无法到达，返回false
   - 更新farthest = max(farthest, i + nums[i])
   - 如果farthest >= n-1，返回true
3. 返回farthest >= n-1

**为什么这样有效？**
- 贪心选择：每一步都尽可能扩展覆盖范围
- 局部最优：当前最远距离
- 全局最优：最终覆盖整个数组

**举例说明：**
```
nums = [2,3,1,1,4]

初始化：farthest=0

i=0: 0 <= 0, farthest = max(0, 0+2) = 2
i=1: 1 <= 2, farthest = max(2, 1+3) = 4 >= 4，返回True ✓

nums = [3,2,1,0,4]

初始化：farthest=0

i=0: 0 <= 0, farthest = max(0, 0+3) = 3
i=1: 1 <= 3, farthest = max(3, 1+2) = 3
i=2: 2 <= 3, farthest = max(3, 2+1) = 3
i=3: 3 <= 3, farthest = max(3, 3+0) = 3 < 4
i=4: 4 > 3，无法到达，返回False ✓
```

**时间复杂度证明：**
- 单次遍历数组：O(n)
- 每个位置常数操作：O(1)
- 总复杂度：O(n)

---

## 45. 跳跃游戏 II

**题目描述：**

给定一个长度为 n 的 0 索引整数数组 nums。初始位置在下标 0。

每个元素 nums[i] 表示从索引 i 向后跳转的最大长度。换句话说，如果你在索引 i 处，你可以跳转到任意 (i + j) 处：

0 <= j <= nums[i] 且 i + j < n

返回到达 n - 1 的最小跳跃次数。测试用例保证可以到达 n - 1。

**测试用例：**

```
示例 1：
输入: nums = [2,3,1,1,4]
输出: 2
解释: 跳到最后一个位置的最小跳跃数是 2。
     从下标为 0 跳到下标为 1 的位置，跳 1 步，然后跳 3 步到达数组的最后一个位置。

示例 2：
输入: nums = [2,3,0,1,4]
输出: 2
```

**最简单实现：**

```python
def jump(nums):
    """
    跳跃游戏II：贪心算法

    思路：
    1. 维护当前覆盖范围[current_end]和下一步能到达的最远位置[next_end]
    2. 每当到达current_end，更新覆盖范围，并增加跳跃次数
    3. 继续直到覆盖终点

    时间复杂度：O(n)
    空间复杂度：O(1)
    """
    if len(nums) <= 1:
        return 0

    n = len(nums)
    jumps = 0  # 跳跃次数
    current_end = 0  # 当前跳跃能到达的最远位置
    next_end = 0  # 下一步跳跃能到达的最远位置

    for i in range(n - 1):  # 不需要到达最后一个位置
        # 更新下一步能到达的最远位置
        next_end = max(next_end, i + nums[i])

        # 如果到达当前跳跃的边界
        if i == current_end:
            jumps += 1
            current_end = next_end

            # 如果已经能到达终点，提前结束
            if current_end >= n - 1:
                break

    return jumps

# BFS版本（另一种思路）
def jumpBFS(nums):
    """
    BFS版本：每一层表示一次跳跃

    时间复杂度：O(n)
    空间复杂度：O(1)
    """
    if len(nums) <= 1:
        return 0

    n = len(nums)
    queue = [(0, 0)]  # (position, jumps)
    visited = {0}

    while queue:
        pos, jumps = queue.pop(0)

        # 如果能到达终点
        if pos >= n - 1:
            return jumps

        # 从当前位置能跳到的所有位置
        for next_pos in range(pos + 1, min(n, pos + nums[pos] + 1)):
            if next_pos not in visited:
                visited.add(next_pos)
                queue.append((next_pos, jumps + 1))

    return -1  # 理论上不会到达这里

# 测试用例
print(jump([2,3,1,1,4]))  # 输出: 2
print(jump([2,3,0,1,4]))  # 输出: 2
```

**解题思路详解：**

这道题的核心是**贪心算法**的应用：

**核心原理：**
- 每一步都选择能跳得最远的策略
- 维护当前覆盖范围和下一步覆盖范围
- 当到达当前范围边界时，进行下一次跳跃

**算法流程：**
1. 初始化jumps=0, current_end=0, next_end=0
2. 遍历数组（到n-2）：
   - 更新next_end = max(next_end, i + nums[i])
   - 如果i == current_end，说明到达边界，进行跳跃
   - jumps += 1, current_end = next_end
3. 返回jumps

**为什么这样有效？**
- 贪心选择：在当前范围内选择最优的跳跃点
- 局部最优：每次跳跃都尽可能扩展覆盖范围
- 全局最优：最小跳跃次数

**举例说明：**
```
nums = [2,3,1,1,4]

初始化：jumps=0, current_end=0, next_end=0

i=0: next_end = max(0, 0+2) = 2
     i==0 == current_end=0，jumps=1, current_end=2

i=1: next_end = max(2, 1+3) = 4
     i==1 != current_end=2，继续

i=2: next_end = max(4, 2+1) = 4
     i==2 != current_end=2，继续

i=3: next_end = max(4, 3+1) = 4
     i==3 != current_end=2，继续

最终jumps=1，但这不对啊...

等等，我重新理解一下算法：

i=0: next_end = max(0, 0+2) = 2
     i==0 == current_end=0，jumps=1, current_end=2

现在current_end=2，意味着从位置0跳一次可以到达位置2

i=1: next_end = max(2, 1+3) = 4
     i==1 != current_end=2，继续

i=2: next_end = max(4, 2+1) = 4
     i==2 == current_end=2，jumps=2, current_end=4 >= 4，结束

最终jumps=2 ✓
```

**时间复杂度证明：**
- 单次遍历数组：O(n)
- 每个位置常数操作：O(1)
- 总复杂度：O(n)

---

## 763. 划分字母区间

**题目描述：**

给你一个字符串 s 。我们要把这个字符串划分为尽可能多的片段，同一字母最多出现在一个片段中。例如，字符串 "ababcc" 能够被分为 ["abab", "cc"]，但类似 ["aba", "bcc"] 或 ["ab", "ab", "cc"] 的划分是非法的。

注意，划分结果需要满足：将所有划分结果按顺序连接，得到的字符串仍然是 s 。

返回一个表示每个字符串片段的长度的列表。

**测试用例：**

```
示例 1：
输入：s = "ababcbacadefegdehijhklij"
输出：[9,7,8]
解释：
划分结果为 "ababcbaca"、"defegde"、"hijhklij" 。
每个字母最多出现在一个片段中。

示例 2：
输入：s = "eccbbbbdec"
输出：[10]
```

**最简单实现：**

```python
def partitionLabels(s):
    """
    划分字母区间：贪心算法

    思路：
    1. 记录每个字母最后出现的位置
    2. 维护当前片段的结束位置
    3. 当遍历到当前片段的结束位置时，划分一个片段

    时间复杂度：O(n)
    空间复杂度：O(1)，因为只有26个字母
    """
    # 记录每个字母最后出现的位置
    last_occurrence = {}
    for i, char in enumerate(s):
        last_occurrence[char] = i

    result = []
    start = 0  # 当前片段的起始位置
    end = 0    # 当前片段的结束位置

    for i, char in enumerate(s):
        # 更新当前片段的结束位置
        end = max(end, last_occurrence[char])

        # 如果到达当前片段的结束位置
        if i == end:
            # 划分一个片段
            result.append(end - start + 1)
            # 更新下一个片段的起始位置
            start = i + 1

    return result

# 测试用例
print(partitionLabels("ababcbacadefegdehijhklij"))  # 输出: [9,7,8]
print(partitionLabels("eccbbbbdec"))                 # 输出: [10]
```

**解题思路详解：**

这道题的核心是**贪心算法**的应用：

**核心原理：**
- 每个字母只能出现在一个片段中
- 片段的划分要尽可能多
- 贪心策略：尽可能早地结束当前片段

**算法流程：**
1. 预处理：记录每个字母最后出现的位置
2. 初始化start=0, end=0
3. 遍历字符串：
   - 更新end = max(end, last_occurrence[char])
   - 如果i == end，说明到达片段边界，划分片段
4. 返回结果列表

**为什么这样有效？**
- 每个字母的最后出现位置确定了它必须被包含的片段范围
- 当前片段必须包含所有字母的最后出现位置
- 贪心选择：当到达当前片段的最大边界时立即划分

**举例说明：**
```
s = "ababcbacadefegdehijhklij"

last_occurrence:
a: 8, b: 6, c: 7, d: 14, e: 15, f: 11, g: 13, h: 19, i: 22, j: 23, k: 20, l: 21

start=0, end=0

i=0, 'a': end = max(0, 8) = 8
i=1, 'b': end = max(8, 6) = 8
i=2, 'a': end = max(8, 8) = 8
i=3, 'b': end = max(8, 6) = 8
i=4, 'c': end = max(8, 7) = 8
i=5, 'b': end = max(8, 6) = 8
i=6, 'a': end = max(8, 8) = 8
i=7, 'c': end = max(8, 7) = 8
i=8, 'a': end = max(8, 8) = 8, i==end，划分[0:9]，start=9

i=9, 'd': end = max(9, 14) = 14
...以此类推...

最终结果：[9,7,8] ✓
```

**时间复杂度证明：**
- 两次遍历字符串：O(n)
- 哈希表操作：O(1)
- 总复杂度：O(n)

---

## 🎯 贪心算法解题技巧总结

### 1. 贪心算法模板
```python
def greedy_template(nums):
    """贪心算法通用模板"""
    # 1. 预处理数据（排序、统计等）
    # 2. 初始化结果变量
    result = []

    # 3. 遍历数据，每次做出贪心选择
    for item in nums:
        # 贪心选择逻辑
        if is_greedy_choice(item):
            result.append(item)

    return result
```

### 2. 股票买卖系列
```python
# 单次交易
def max_profit_once(prices):
    min_price = float('inf')
    max_profit = 0

    for price in prices:
        min_price = min(min_price, price)
        max_profit = max(max_profit, price - min_price)

    return max_profit

# 多次交易
def max_profit_multiple(prices):
    profit = 0
    for i in range(1, len(prices)):
        if prices[i] > prices[i-1]:
            profit += prices[i] - prices[i-1]
    return profit
```

### 3. 跳跃游戏系列
```python
# 能否到达终点
def can_reach_end(nums):
    farthest = 0
    for i in range(len(nums)):
        if i > farthest:
            return False
        farthest = max(farthest, i + nums[i])
    return farthest >= len(nums) - 1

# 最小跳跃次数
def min_jumps(nums):
    jumps = 0
    current_end = 0
    next_end = 0

    for i in range(len(nums) - 1):
        next_end = max(next_end, i + nums[i])
        if i == current_end:
            jumps += 1
            current_end = next_end
    return jumps
```

### 4. 区间问题
```python
# 区间合并
def merge_intervals(intervals):
    if not intervals:
        return []

    intervals.sort(key=lambda x: x[0])
    merged = [intervals[0]]

    for current in intervals[1:]:
        if current[0] <= merged[-1][1]:
            merged[-1][1] = max(merged[-1][1], current[1])
        else:
            merged.append(current)

    return merged

# 活动安排
def activity_selection(activities):
    activities.sort(key=lambda x: x[1])  # 按结束时间排序
    selected = [activities[0]]

    for activity in activities[1:]:
        if activity[0] >= selected[-1][1]:
            selected.append(activity)

    return selected
```

### 💡 解题技巧

1. **验证贪心正确性**：
   - 证明局部最优能导致全局最优
   - 寻找反例验证
   - 数学归纳法证明

2. **常见贪心模式**：
   - **排序后处理**：区间问题、任务调度
   - **维护最优值**：股票买卖、跳跃游戏
   - **贪心选择**：硬币找零、背包问题（部分情况）

3. **边界条件处理**：
   - 空数组、空字符串
   - 单个元素
   - 所有元素相同的情况

贪心算法简单高效，但需要仔细验证其正确性。掌握常见贪心模式的套路，能快速解决很多问题！
