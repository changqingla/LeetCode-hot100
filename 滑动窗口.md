# 滑动窗口专题

## 🔍 滑动窗口基础知识

### 📖 定义

**滑动窗口**（Sliding Window）是一种基于双指针的技术，用于在数组或字符串上进行高效的子数组/子串搜索。通过维护一个窗口（通常用两个指针表示窗口的左右边界），可以在O(n)时间复杂度内解决许多子数组/子串相关问题。

### ⚡ 核心特征

1. **窗口扩张**：右指针向右移动，扩大窗口
2. **窗口收缩**：左指针向右移动，缩小窗口
3. **窗口维护**：在扩张和收缩过程中维护窗口的某种性质

### 🎯 解题步骤

1. **确定窗口性质**：明确窗口需要满足的条件
2. **初始化窗口**：设置左右指针的初始位置
3. **移动右指针**：扩张窗口，直到满足条件
4. **移动左指针**：当不满足条件时，收缩窗口
5. **更新结果**：在每次有效窗口时更新答案

### 🐍 滑动窗口的应用场景

- **子串问题**：最长无重复子串、最小覆盖子串
- **子数组问题**：和为k的子数组、最小长度子数组
- **字符串匹配**：查找异位词、模式匹配
- **数组操作**：最大连续和、定长子数组的最大值

### 📏 窗口类型

#### 1. 定长窗口
```python
# 窗口大小固定为k
def fixed_window(nums, k):
    left = 0
    window_sum = sum(nums[:k])
    max_sum = window_sum

    for right in range(k, len(nums)):
        window_sum = window_sum - nums[left] + nums[right]
        left += 1
        max_sum = max(max_sum, window_sum)

    return max_sum
```

#### 2. 不定长窗口
```python
# 窗口大小动态变化
def variable_window(nums, target):
    left = 0
    current_sum = 0
    min_length = float('inf')

    for right in range(len(nums)):
        current_sum += nums[right]

        # 收缩窗口
        while current_sum >= target:
            min_length = min(min_length, right - left + 1)
            current_sum -= nums[left]
            left += 1

    return min_length if min_length != float('inf') else 0
```

---

## 📋 题目目录

| 题号 | 题目名称 | 难度 | 核心技巧 |
|------|----------|------|----------|
| 3 | 无重复字符的最长子串 | 中等 | 滑动窗口+哈希表 |
| 438 | 找到字符串中所有字母异位词 | 中等 | 滑动窗口+字符计数 |

---

## 3. 无重复字符的最长子串

**题目描述：**

给定一个字符串 s ，请你找出其中不含有重复字符的 最长子串 的长度。

**测试用例：**

```
示例 1：
输入: s = "abcabcbb"
输出: 3
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。

示例 2：
输入: s = "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。

示例 3：
输入: s = "pwwkew"
输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
请注意，你的答案必须是 子串 的长度，"pwke" 是一个子序列，不是子串。
```

**最简单实现：**

```python
def lengthOfLongestSubstring(s):
    """
    使用滑动窗口+哈希表求解无重复字符的最长子串
    
    思路：
    1. 使用哈希表记录字符最后出现的位置
    2. 使用左指针维护无重复子串的起始位置
    3. 右指针遍历字符串，遇到重复字符时移动左指针
    4. 实时更新最大长度
    
    时间复杂度：O(n)
    空间复杂度：O(min(m,n))，其中m是字符集大小
    """
    if not s:
        return 0
    
    # 哈希表记录字符最后出现的位置
    char_index = {}
    left = 0
    max_length = 0
    
    for right in range(len(s)):
        # 如果当前字符已经在窗口中出现过
        if s[right] in char_index:
            # 移动左指针到重复字符的下一个位置
            # 确保left不会回退
            left = max(left, char_index[s[right]] + 1)
        
        # 更新字符的位置
        char_index[s[right]] = right
        
        # 更新最大长度
        max_length = max(max_length, right - left + 1)
    
    return max_length

# 测试用例
print(lengthOfLongestSubstring("abcabcbb"))  # 输出: 3
print(lengthOfLongestSubstring("bbbbb"))     # 输出: 1
print(lengthOfLongestSubstring("pwwkew"))    # 输出: 3
```

**解题思路详解：**

这道题的核心是使用**滑动窗口 + 哈希表**来解决：

**核心原理：维护一个无重复字符的窗口**
- 窗口[left, right]内的字符都各不相同
- 当遇到重复字符时，收缩左边界直到重复消除

**算法流程：**
1. 初始化left=0, char_index={}, max_length=0
2. 右指针遍历字符串：
   - 如果s[right]已在哈希表中，说明重复
   - 移动left到重复字符位置+1（确保left不回退）
   - 更新哈希表中s[right]的位置
   - 更新最大长度

**哈希表的妙用：**
- 记录每个字符最后出现的位置
- 快速判断是否重复
- 计算左指针应该移动到的位置

**举例说明：**
```
s = "abcabcbb"

i=0: s[0]='a', char_index={'a':0}, left=0, length=1, max=1
i=1: s[1]='b', char_index={'a':0,'b':1}, left=0, length=2, max=2  
i=2: s[2]='c', char_index={'a':0,'b':1,'c':2}, left=0, length=3, max=3
i=3: s[3]='a', 'a'已在char_index中，left=max(0,0+1)=1, char_index={'a':3,'b':1,'c':2}, length=3-1+1=3, max=3
i=4: s[4]='b', 'b'已在char_index中，left=max(1,1+1)=2, char_index={'a':3,'b':4,'c':2}, length=4-2+1=3, max=3
i=5: s[5]='c', 'c'已在char_index中，left=max(2,2+1)=3, char_index={'a':3,'b':4,'c':5}, length=5-3+1=3, max=3
i=6: s[6]='b', 'b'已在char_index中，left=max(3,4+1)=5, char_index={'a':3,'b':6,'c':5}, length=6-5+1=2, max=3
i=7: s[7]='b', 'b'已在char_index中，left=max(5,6+1)=7, char_index={'a':3,'b':7,'c':5}, length=7-7+1=1, max=3

最终结果：3
```

**时间复杂度证明：**
- 右指针遍历一次字符串：O(n)
- 左指针最多也遍历一次字符串：O(n)
- 哈希表操作：O(1)
- 总复杂度：O(n)

---

## 438. 找到字符串中所有字母异位词

**题目描述：**

给定两个字符串 s 和 p，找到 s 中所有 p 的 异位词 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。

异位词 指字母相同，但排列不同的字符串。

**测试用例：**

```
示例 1：
输入: s = "cbaebabacd", p = "abc"
输出: [0,6]
解释:
起始索引等于 0 的子串是 "cba", 它是 "abc" 的异位词。
起始索引等于 6 的子串是 "bac", 它是 "abc" 的异位词。

示例 2：
输入: s = "abab", p = "ab"
输出: [0,1,2]
解释:
起始索引等于 0 的子串是 "ab", 它是 "ab" 的异位词。
起始索引等于 1 的子串是 "ba", 它是 "ab" 的异位词。
起始索引等于 2 的子串是 "ab", 它是 "ab" 的异位词。
```

**最简单实现：**

```python
from collections import Counter

def findAnagrams(s, p):
    """
    使用滑动窗口+字符计数求解字符串异位词问题
    
    思路：
    1. 使用两个Counter统计p和当前窗口的字符频率
    2. 滑动窗口大小固定为len(p)
    3. 当窗口内字符频率与p完全相同时，记录起始索引
    4. 使用优化技巧：维护一个匹配计数，避免每次比较整个Counter
    
    时间复杂度：O(n)
    空间复杂度：O(1)，因为字符集大小固定
    """
    if len(s) < len(p):
        return []
    
    # 统计p的字符频率
    p_count = Counter(p)
    # 统计当前窗口的字符频率
    window_count = Counter()
    
    result = []
    left = 0
    
    # 初始化窗口
    for right in range(len(p)):
        window_count[s[right]] += 1
    
    # 检查初始窗口
    if window_count == p_count:
        result.append(0)
    
    # 滑动窗口
    for right in range(len(p), len(s)):
        # 移除左边字符
        window_count[s[left]] -= 1
        if window_count[s[left]] == 0:
            del window_count[s[left]]
        left += 1
        
        # 添加右边字符
        window_count[s[right]] += 1
        
        # 检查是否匹配
        if window_count == p_count:
            result.append(left)
    
    return result

# 测试用例
print(findAnagrams("cbaebabacd", "abc"))  # 输出: [0,6]
print(findAnagrams("abab", "ab"))         # 输出: [0,1,2]
```

**优化版本（使用匹配计数）：**

```python
from collections import Counter

def findAnagrams(s, p):
    """
    优化版本：使用匹配计数避免每次比较Counter
    
    思路：
    1. 统计p的字符频率
    2. 使用match_count记录完全匹配的字符种类数
    3. 当某个字符的频率匹配时，match_count+1
    4. 当match_count等于字符种类数时，说明找到异位词
    
    时间复杂度：O(n)
    空间复杂度：O(1)
    """
    if len(s) < len(p):
        return []
    
    p_count = Counter(p)
    window_count = Counter()
    result = []
    
    # 需要匹配的字符种类数
    match_count = 0
    required_matches = len(p_count)
    
    left = 0
    
    for right in range(len(s)):
        # 添加右边字符
        char = s[right]
        window_count[char] += 1
        
        # 如果这个字符的频率现在匹配了
        if char in p_count and window_count[char] == p_count[char]:
            match_count += 1
        
        # 窗口形成后，开始移动左指针
        if right >= len(p) - 1:
            # 如果完全匹配，记录结果
            if match_count == required_matches:
                result.append(left)
            
            # 移除左边字符
            left_char = s[left]
            if left_char in p_count and window_count[left_char] == p_count[left_char]:
                match_count -= 1
            window_count[left_char] -= 1
            left += 1
    
    return result

# 测试用例
print(findAnagrams("cbaebabacd", "abc"))  # 输出: [0,6]
print(findAnagrams("abab", "ab"))         # 输出: [0,1,2]
```

**解题思路详解：**

这道题的核心是使用**滑动窗口 + 字符计数**来解决：

**核心原理：异位词的特征**
- 两个字符串是异位词当且仅当它们的字符频率完全相同
- 我们需要在s中找到所有长度为len(p)的子串，其字符频率与p相同

**为什么使用滑动窗口：**
1. **定长窗口**：窗口大小固定为len(p)
2. **高效检查**：使用Counter或哈希表快速比较字符频率
3. **O(n)复杂度**：每个字符只被处理常数次

**算法流程（优化版本）：**
1. 统计p的字符频率p_count
2. 初始化match_count=0, required_matches=len(p_count)
3. 右指针遍历字符串：
   - 添加s[right]到窗口
   - 如果该字符频率匹配，match_count++
   - 当窗口长度>=len(p)时：
     - 如果match_count==required_matches，记录left
     - 移除s[left]，更新match_count
     - left++

**匹配计数的妙用：**
- match_count记录当前完全匹配的字符种类数
- 当match_count == len(p_count)时，说明找到异位词
- 避免了每次比较整个哈希表的开销

**举例说明：**
```
s = "cbaebabacd", p = "abc"
p_count = {'a':1, 'b':1, 'c':1}, required_matches=3

初始化窗口 [c,b,a]：
window_count = {'c':1, 'b':1, 'a':1}, match_count=3
match_count==3，记录索引0

滑动到 [b,a,e]：
移除'c'：window_count={'b':1,'a':1,'e':1}, match_count=2 (c不再匹配)
添加'e'：window_count={'b':1,'a':1,'e':1}, match_count=2

滑动到 [a,e,b]：
移除'b'：window_count={'a':1,'e':1}, match_count=1
添加'b'：window_count={'a':1,'e':1,'b':1}, match_count=2

滑动到 [e,b,a]：
移除'a'：window_count={'e':1,'b':1}, match_count=1
添加'a'：window_count={'e':1,'b':1,'a':1}, match_count=2

滑动到 [b,a,b]：
移除'e'：window_count={'b':1,'a':1}, match_count=1
添加'b'：window_count={'b':2,'a':1}, match_count=1

滑动到 [a,b,a]：
移除'b'：window_count={'a':1,'b':1}, match_count=2
添加'a'：window_count={'a':2,'b':1}, match_count=2

滑动到 [b,a,c]：
移除'a'：window_count={'b':1,'a':1}, match_count=1
添加'c'：window_count={'b':1,'a':1,'c':1}, match_count=3
match_count==3，记录索引6

最终结果：[0,6]
```

**时间复杂度证明：**
- 右指针遍历一次字符串：O(n)
- 每个字符的添加/删除操作：O(1)
- 哈希表操作：O(1)
- 总复杂度：O(n)

---

## 🎯 滑动窗口解题模板总结

### 1. 定长窗口模板
```python
def fixed_window(nums, k):
    """定长窗口：窗口大小固定"""
    if len(nums) < k:
        return 0
    
    # 初始化窗口
    window_sum = sum(nums[:k])
    max_sum = window_sum
    
    # 滑动窗口
    for right in range(k, len(nums)):
        window_sum = window_sum - nums[right - k] + nums[right]
        max_sum = max(max_sum, window_sum)
    
    return max_sum
```

### 2. 不定长窗口模板
```python
def variable_window(nums, target):
    """不定长窗口：窗口大小动态变化"""
    left = 0
    current_sum = 0
    min_length = float('inf')
    
    for right in range(len(nums)):
        current_sum += nums[right]
        
        # 收缩窗口条件
        while current_sum >= target and left <= right:
            min_length = min(min_length, right - left + 1)
            current_sum -= nums[left]
            left += 1
    
    return min_length if min_length != float('inf') else 0
```

### 3. 字符串匹配窗口模板
```python
from collections import Counter

def string_window(s, p):
    """字符串匹配：使用字符计数"""
    if len(s) < len(p):
        return []
    
    p_count = Counter(p)
    window_count = Counter()
    result = []
    left = 0
    
    for right in range(len(s)):
        window_count[s[right]] += 1
        
        if right >= len(p) - 1:
            if window_count == p_count:
                result.append(left)
            
            # 移除左边字符
            if window_count[s[left]] == 1:
                del window_count[s[left]]
            else:
                window_count[s[left]] -= 1
            left += 1
    
    return result
```

滑动窗口是解决子数组/子串问题的强大工具，掌握这三种模板可以解决大部分相关问题。